import { Injectable } from '@nestjs/common';
@Injectable()export class {{ServiceEndpoint}}Service {
  async process(): Promise<{{ServiceEndpointModel}}> {
    const canProceed = await this.preProcess({{maybe something here after we do the pre and post process}});

    if (!canProceed) {
      throw new Error('Service cannot proceed'); // Replace with your custom error message
    }

    const result = await this.execute();

    await this.postProcess();

    return result;
  }
}

/////
const templateData = {
  ServiceEndpoint: 'UserService', // Replace with your desired service endpointServiceEndpointModel: 'User', // Replace with the corresponding model
};
 